# Embedding 工程化平衡：维度 × 精度 × 成本（补充参考）

> 这份不是今天必须看完的"学习任务"，而是建立工程化思维的底座。
> 周六写 ADR「知识服务层架构选型」时会直接用到这里的决策框架。
> 你的优势：这些模式在 LLM 平台/ASR 中台里都有对应物，不是从零学。

---

## 核心问题

向量维度越高 → 语义表达越精准 → 但存储、内存、计算、网络传输的压力线性增长。

**100 篇文档不用担心，100 万篇呢？1 亿篇呢？** 平台级服务必须在精度和成本之间找到工程平衡点。

---

## 第一层：模型选型时定维度

不是"越高越好"，而是按场景选：

| 场景 | 典型维度 | 理由 |
|------|----------|------|
| 短文本匹配（query↔标题） | 384-768 | 短文本语义信息量有限，高维度浪费 |
| 长文档检索（RAG） | 1024-1536 | 需要更细粒度语义区分 |
| 多语言/跨模态 | 1024+ | 要同时编码多种语言/模态的语义空间 |
| 粗召回阶段（两阶段架构） | 128-256 | 只求大方向对，不求精确 |

**平台视角**：给 100 个业务方提供服务时，不可能强制所有人用同一维度。需要提供多档选择（类似你的 LLM 平台提供多个模型供业务方按场景选）。

---

## 第二层：存储优化——不改维度，改数值精度

| 方法 | 压缩比 | 精度损失 | 原理 |
|------|--------|----------|------|
| float32 → float16 | 2× | 极小 | 半精度浮点，大部分场景够用 |
| float32 → int8（标量量化） | 4× | 小（<1% 召回下降） | 每维用 8bit 整数表示 |
| float32 → binary（二值量化） | 32× | 较大（5-10%） | 每维只存 0/1，用汉明距离替代余弦 |

**规模感知**：

```
100 万条 × 1024 维 × float32 = 4 GB
100 万条 × 1024 维 × int8    = 1 GB
100 万条 × 1024 维 × binary  = 128 MB

1 亿条 × 1024 维 × float32   = 400 GB（需要分布式存储）
1 亿条 × 1024 维 × int8      = 100 GB（单机大内存可撑）
```

**类比你的经验**：和模型推理量化（INT8/FP16 推理加速）是同一套思路——牺牲微小精度换大幅资源节省。你做 LLM 平台时模型部署选 FP16 还是 INT8，和这里向量存储选 float32 还是 int8 是完全同构的决策。

---

## 第三层：索引结构——不改向量，改搜索方式

暴力搜索 O(N) 在百万级以上不可行（100 万条 × 1024 维逐一算余弦 ≈ 几十秒）。向量库用 ANN（近似最近邻）索引加速：

### HNSW（分层导航小世界图）

```
原理：建多层图结构
  - 顶层：少量节点，长距离跳跃（粗定位）
  - 底层：全部节点，短距离连接（精确搜索）
  - 查询从顶层入口开始，逐层下沉，每层贪心找最近邻

优点：查询快（O(logN)）、召回率高（>95%）
缺点：内存占用大（图结构常驻内存，约为原始向量的 1.5-2 倍）
适合：百万~千万级，对延迟敏感（<10ms）
```

**类比**：就像你做负载均衡时的"多级路由"——先粗筛（顶层跳），再细查（底层遍历）。也像 DNS 解析：根域名服务器 → 顶级域 → 权威域名服务器，逐层缩小范围。

### IVF（倒排文件索引）

```
原理：先用 K-Means 把所有向量聚成 nlist 个桶
  - 查询时只搜最近的 nprobe 个桶（而不是全部）
  - nprobe 越大越精确但越慢

优点：内存占用小（只需存聚类中心 + 原始向量）
缺点：需要训练聚类中心；边界向量可能被漏掉
适合：千万~亿级，数据可以放磁盘
```

**类比**：就像数据库分区（partition by hash）——查询只扫相关分区，不全表扫描。

### PQ（乘积量化）

```
原理：把 1024 维切成 M 段（如 M=64，每段 16 维）
  - 每段独立做聚类（256 个中心），用 1 字节编码所属中心
  - 1024 维 float32（4096 字节）→ 64 字节（压缩 64 倍）
  - 查询时用查表法快速估算距离

优点：极致压缩，适合超大规模
缺点：精度损失较大，通常与 IVF 组合使用（IVF-PQ）
适合：亿级以上，内存受限
```

### 索引选型速查

| 数据规模 | 推荐索引 | 延迟目标 | 内存需求 |
|----------|----------|----------|----------|
| <10 万 | 暴力搜索（flat） | <50ms | 低 |
| 10 万~500 万 | HNSW | <10ms | 高（全量内存） |
| 500 万~5000 万 | IVF-HNSW 或 IVF-FLAT | <50ms | 中 |
| >5000 万 | IVF-PQ + 精排 | <100ms | 低 |

---

## 第四层：两阶段检索（工程中最主流的方案）

```
┌─────────────────────────────────────────────────────┐
│  Stage 1：粗召回（Recall）                            │
│  - 输入：query 向量                                   │
│  - 方法：低精度索引（binary/PQ）或降维向量             │
│  - 输出：Top-100~500 候选                             │
│  - 目标：不漏（高召回率），允许不精确                   │
│  - 延迟：< 10ms                                      │
├─────────────────────────────────────────────────────┤
│  Stage 2：精排（Re-rank）                             │
│  - 输入：候选集 + 原始 float32 向量                   │
│  - 方法：精确余弦相似度 / Cross-encoder 模型重打分     │
│  - 输出：Top-K（3~10 条）                             │
│  - 目标：排序精确                                     │
│  - 延迟：< 20ms（候选少，暴力算很快）                  │
└─────────────────────────────────────────────────────┘
```

**为什么这是主流**：单阶段要么快但不准（纯 ANN），要么准但不快（暴力全量精排）。两阶段把两者优势组合——ANN 负责缩小范围，精排负责最终质量。

**类比你的经验**：
- ASR：声学模型粗解码（beam search 出 N-best）→ 语言模型重打分（rescore）→ 输出最终结果
- LLM 平台：模型路由粗筛（按能力/成本选候选模型）→ 精排（按实际 prompt 复杂度最终选定）
- 搜索引擎：召回层（倒排索引/向量检索）→ 排序层（Learning to Rank）

**这是同一个架构模式在不同领域的实例。** 你已经有直觉了，只是没在向量检索场景下显式用过。

---

## 第五层：Matryoshka Embedding（套娃嵌入，2024-2025 新趋势）

### 核心思想

训练时就让向量的**前 N 维承载最重要的语义信息**，后面的维度是"细节补充"。这样可以灵活截断：

```
完整向量（1024 维）：[████████████████████████████████] 精度 100%
截取前 512 维：      [████████████████]                  精度 ~97%
截取前 256 维：      [████████]                          精度 ~93%
截取前 128 维：      [████]                              精度 ~88%
```

### 工程应用

| 阶段 | 用多少维 | 目的 |
|------|----------|------|
| Stage 1 粗召回 | 前 128 维 | 快速从百万文档中筛出候选（存储小、计算快） |
| Stage 2 精排 | 全 1024 维 | 对候选精确排序 |

**好处**：同一套 embedding 调用，不需要跑两次模型；同一份存储，粗召回和精排用不同前缀长度。

### text-embedding-v4 支持 Matryoshka

你用的百炼 text-embedding-v4 支持通过 `dimensions` 参数指定输出维度：

```python
# 默认 1024 维
resp = client.embeddings.create(model="text-embedding-v4", input=["hello"])

# 指定 256 维（Matryoshka 截断，精度略降但存储/计算降 4 倍）
resp = client.embeddings.create(model="text-embedding-v4", input=["hello"], dimensions=256)
```

本周实验你可以试试：同样的 3 句话，1024 维 vs 256 维的余弦相似度排序结果是否一致。（大概率一致，因为 3 句话区分度很大。）

---

## 决策框架总结（周六 ADR 直接用）

当你要为平台选 embedding + 向量检索方案时，按这四步决策：

```
Step 1: 业务精度需求
  → 决定模型和维度下限（"召回率必须 >95% 还是 >85% 就够？"）

Step 2: 数据规模
  → 决定索引类型（万级=flat，百万级=HNSW，亿级=IVF-PQ）

Step 3: 延迟 SLA
  → 决定是否需要两阶段（<10ms 必须 ANN，<50ms 可以单阶段暴力）

Step 4: 成本预算
  → 决定存储精度（float32 贵但准，int8 省 4 倍但微损，binary 省 32 倍但粗）
```

**这四步不是独立的，互相约束**：比如延迟要求 <5ms + 数据 1 亿条 + 召回率 >95% → 必须 HNSW + float16 + 多副本 → 内存成本爆炸 → 要么降召回率，要么接受更高延迟，要么加预算。

**这就是工程——没有最优解，只有约束条件下的最佳 trade-off。** 和你做 LLM 平台时"推理延迟 vs GPU 成本 vs 模型精度"的三角平衡完全同构。

---

## 和你已有经验的完整映射

| Embedding 工程决策 | 你在 LLM 平台/语音中台做过的同构问题 |
|---|---|
| 维度选择（精度 vs 成本） | 模型选择（大模型准但贵 vs 小模型快但粗） |
| 向量量化（float32→int8） | 推理量化（FP32→FP16→INT8） |
| ANN 索引（HNSW/IVF） | 模型路由（按特征快速匹配最合适的模型） |
| 两阶段检索（粗召回+精排） | ASR N-best rescore / 搜索召回+排序 |
| Matryoshka（灵活截断） | 模型蒸馏（大模型→小模型，保留核心能力） |
| 多租户向量隔离 | 多业务线共用 LLM 平台的隔离方案 |
| embedding 调用成本 | token 计量与成本归因（你最强的领域） |

**面试时这样讲**："向量检索服务的工程平衡和我在 LLM 平台做的模型推理优化是同构问题——都是精度、延迟、成本三角约束下找最优解。我的优势是已经在生产环境（日均 5000 亿 token）做过这类 trade-off，不是纸上谈兵。"
