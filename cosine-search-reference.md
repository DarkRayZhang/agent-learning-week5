# 周二精简参考：余弦相似度原理 + 检索原型（20min 阅读）

> 读完合上，自己写检索函数。不要抄代码，理解后凭记忆写。

---

## 1. 余弦相似度——手算一遍

### 公式拆解

```
cos(A, B) = (A · B) / (|A| × |B|)
```

拆成三步：

| 步骤 | 操作 | 含义 |
|------|------|------|
| ① A · B | Σ(aᵢ × bᵢ)，逐位相乘再求和 | 点积：两个向量"方向一致程度"的原始分 |
| ② \|A\| | √(Σaᵢ²)，每位平方求和再开根 | 模/范数：向量的"长度" |
| ③ 除法 | 点积 / (模A × 模B) | 归一化：消除长度影响，只保留方向 |

### 用 2 维向量手算一遍（直觉建立）

假设 A = [3, 4]，B = [6, 8]，C = [4, -3]

```
A · B = 3×6 + 4×8 = 18 + 32 = 50
|A|   = √(9+16) = √25 = 5
|B|   = √(36+64) = √100 = 10
cos(A,B) = 50 / (5×10) = 1.0  ← 完全同方向（B 就是 A 的 2 倍）

A · C = 3×4 + 4×(-3) = 12 - 12 = 0
cos(A,C) = 0 / (5×5) = 0.0  ← 正交，完全无关
```

**关键洞察**：A 和 B 数值完全不同（[3,4] vs [6,8]），但余弦相似度 = 1.0，因为方向一致。这就是"只看方向不看长度"的含义。

### 实际 1024 维的情况

原理一模一样，只是从 2 个数变成 1024 个数逐位算。`embed_demo.py` 里的 `cosine()` 函数就是这三步的代码实现——回看那 4 行，现在应该完全能读懂。

---

## 2. 归一化（Normalization）

### 什么是归一化

把一个向量缩放到**模 = 1**（单位向量），方向不变：

```
A_normalized = A / |A|

例：A = [3, 4]，|A| = 5
A_normalized = [3/5, 4/5] = [0.6, 0.8]
验证：√(0.36 + 0.64) = √1 = 1 ✓
```

### 归一化后的好处

| 场景 | 不归一化 | 归一化后 |
|------|----------|----------|
| 比较相似度 | 必须用余弦公式（含除法） | 直接算点积就行（因为分母=1） |
| 向量库存储 | 存原始向量 | 存归一化向量，检索时用点积（更快） |
| 排序结果 | 余弦排序 | 点积排序（结果完全一致） |

**一句话**：归一化 = 预先除掉长度，之后点积就等价于余弦。很多向量库（包括 Chroma）内部会帮你归一化。

### 为什么 embedding 模型不直接输出归一化向量？

有些会（比如 OpenAI 的 text-embedding-3 系列默认归一化），有些不会。text-embedding-v4（百炼）输出的向量**不保证归一化**，所以你自己算相似度时需要用完整余弦公式（带除法那步），或者手动归一化后再用点积。

---

## 3. 最朴素的检索原型

### 核心思路（5 步）

```
① 准备数据集：N 条短语/文档
② 全部 embedding：每条 → 一个向量，存起来（这就是你的"库"）
③ 用户输入 query
④ query embedding：query → 一个向量
⑤ 遍历库内所有向量，逐一算余弦相似度，排序，返回 Top-K
```

这就是**暴力检索（brute-force search）**——逐一比较，O(N) 复杂度。N=10 或 N=1000 时完全够用；N=百万时需要 ANN（近似最近邻）索引加速，那是向量库干的事（周三学）。

### 代码结构（伪代码，自己写实现）

```python
# 你需要写的函数结构：

def build_index(texts: list[str]) -> list[list[float]]:
    """把 N 条文本全部 embedding，返回向量列表"""
    # 调用 embedding API，逐条或批量
    pass

def search(query: str, texts: list[str], vectors: list[list[float]], top_k: int = 3):
    """query 转向量 → 与库内每条算余弦 → 排序 → 返回 Top-K"""
    # 1. query embedding
    # 2. 逐一算 cosine similarity
    # 3. 排序（从高到低）
    # 4. 取前 top_k 个，返回文本+分数
    pass
```

### 实现提示

- embedding 调用可以复用 `embed_demo.py` 里的 `embed()` 函数（已验证能跑）
- cosine 函数也直接复用
- 批量 embedding：API 支持 `input=["文本1", "文本2", ...]` 一次传多条
  - **规范来源**：OpenAI 的 `POST /v1/embeddings`（事实标准，全行业"OpenAI 兼容"都照抄这个 schema）
  - **你的代码走的是**：`openai` Python SDK 的 `client.embeddings.create(...)`，它把这次调用拼成 `POST {EMBEDDING_BASE_URL}/embeddings`
  - **实际打到的服务器**：`.env` 里 `EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1` → **阿里云百炼**
  - 所以：**参数名是 OpenAI 定的，限制是阿里云定的**
  - ⚠️ 百炼 text-embedding-v4 单次 **最多 10 条**、单行 ≤ 8192 token，超了必须自己切块循环（SDK 不会帮你切）
  - 返回 `resp.data` 是列表，按**输入顺序**排列，每条带 `index`；取值别假设顺序，用 `item.index` 回对应原文更稳
  - ⚠️「省钱」要分清：同步批量只是少几次 HTTP 往返；**真正的半价是百炼另一套异步 Batch 接口**（0.00025 元/千 token，普通 0.0005 元），不是把 input 传成数组就自动半价
- 排序：`sorted(results, key=lambda x: x[1], reverse=True)[:top_k]`

---

## 4. 今天的实验设计

### 数据集要求

造 **10 条中文短语**，覆盖 2-3 个主题，让语义检索有区分度。建议：

```
主题 A（宠物）：猫粮推荐 / 狗狗生病怎么办 / 宠物医院哪家好
主题 B（编程）：Python 怎么读文件 / Java 和 Python 的区别 / 代码报错了怎么调试
主题 C（天气）：今天天气怎么样 / 明天下不下雨 / 北京气温多少度
随意一条：周末去哪里玩
```

### 验证 query

| query | 期望 Top-1 命中主题 |
|-------|---------------------|
| "宠物食品" | 主题 A（猫粮推荐） |
| "小猫吃什么" | 主题 A |
| "程序出错" | 主题 B（代码报错） |
| "气温" | 主题 C |
| "动物" | 主题 A |

关键观察：**"宠物食品"和"猫粮推荐"没有任何共同关键词**，但语义检索应该能命中——这就是 embedding 检索 vs 关键词检索的本质区别（W4 的 `search_kb` 按空格 split 切词搜不到，现在能搜到了）。

---

## 5. 平台思考题（碎片时间 5min，记 2-3 行到 notes）

> 你的 LLM 平台有没有"检索"类能力？如果要给 100 个业务方提供向量检索服务，和你现在做的有什么异同？

提示方向（不要照抄，想自己的经验）：
- 你的 LLM 平台现在是"请求→模型→响应"的直通模式，有没有需要检索知识库再送给模型的场景？
- 100 个业务方共用一个向量检索服务 vs 各自部署，trade-off 是什么？（隔离、成本、运维复杂度）
- 和你做过的模型路由有没有相似之处？（都是"根据输入特征选择最匹配的资源"）

---

## 6. 读完自检

1. 余弦公式三步能默写出来吗？（点积 → 算模 → 除）
2. 归一化后为什么点积 = 余弦？
3. 暴力检索 5 步流程能说出来吗？
4. "宠物食品"能命中"猫粮推荐"，但 W4 的关键词检索搜不到——为什么？
