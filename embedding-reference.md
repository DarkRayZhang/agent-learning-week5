# Embedding 精简参考（20min 阅读）

> 读完这份后，合上去 notes.md 用自己的话写。不要抄。

---

## 1. 一句话定义

**Embedding = 把文本映射成一个固定长度的浮点数数组（向量），使得语义相近的文本在向量空间中距离近。**

类比你的经验：就像给每条文本算一个"语义坐标"。你知道搜索引擎的倒排索引是按词匹配，embedding 是按意思匹配——"猫粮"和"宠物食品"没有任何共同关键词，但向量坐标很近。

---

## 2. Token → Embedding 的 Pipeline

```
原始文本           "狗是人类的好朋友"
    ↓ Tokenizer（分词/切片）
Token 序列         ["狗", "是", "人类", "的", "好", "朋友"]  ← 每个 token 是一个整数 ID
    ↓ Embedding 模型（神经网络前向传播）
Token 向量         每个 token ID → 一个初始向量（查表）→ 经过 Transformer 层 → 带上下文语义的向量
    ↓ Pooling（聚合）
句向量             把所有 token 向量压缩成一个固定维度向量（如 1024 维）
```

关键认知：

- **Token 是输入单位，不是输出单位**。embedding 模型的输入是 token 序列，输出是整段文本的一个向量。
- **Pooling 方式决定"一段文本怎么变成一个向量"**：常见有 mean pooling（所有 token 向量取平均）、CLS pooling（取特殊 [CLS] token 的输出）。你不需要关心具体实现，只需要知道：不管文本多长（在模型上限内），输出维度是固定的。
- **维度由模型决定，不是由文本长度决定**。text-embedding-v4 输出 1024 维，不管你输入 3 个字还是 3000 个字。

### 和 W1 学的 token 概念的关系

| 概念 | W1 学的（chat 模型） | W5 的（embedding 模型） |
|------|---------------------|------------------------|
| Token | LLM 的输入/输出计费单位 | Embedding 模型的输入单位（也计费） |
| 上下文窗口 | Chat 模型一次能处理的最大 token 数 | Embedding 模型一次能处理的最大 token 数（通常 8192） |
| 输出 | 文本（token 序列） | 一个固定长度的浮点数数组 |

**一句话总结**：token 是文本被切片后的最小单位；embedding 是把一段 token 序列"压缩"成一个向量。token 是原料，embedding 是产物。

---

## 3. 为什么语义近的向量距离近？

这不是规则，是**训练出来的**。

训练过程（对比学习，Contrastive Learning）：

1. 准备大量"语义相近的句对"（正样本）和"语义无关的句对"（负样本）
2. 训练目标：让正样本的向量距离越来越近，负样本的向量距离越来越远
3. 经过数十亿句对训练后，模型学会了"把意思相近的文本映射到空间中相邻的位置"

**你不需要理解训练细节，只需要知道**：这是一个统计学习的结果，不是硬编码的规则。所以它有时候会犯错（比如反讽、专业术语），但大方向上"意思近 = 距离近"是成立的。

你刚才跑的 demo 就是证据：
- 狗↔宠物 = 0.5955（语义相关）
- 狗↔汽车 = 0.3180（语义无关）

---

## 4. 向量维度是什么

**维度 = 那个浮点数数组的长度。**

- text-embedding-v4 → 1024 维 → 每个文本变成 1024 个浮点数
- OpenAI text-embedding-3-small → 1536 维
- BGE-M3 → 1024 维

直觉理解：维度越高，能表达的语义细节越多（就像描述一个人，用 3 个属性 vs 用 100 个属性，后者更精确）。但代价是存储更大、计算更慢。

**对你的实际影响**：

| 维度 | 100 篇文档 × 10 块/篇 | 存储 |
|------|------------------------|------|
| 1024 维 | 1000 个向量 × 1024 × 4 bytes | ~4 MB |
| 1536 维 | 1000 个向量 × 1536 × 4 bytes | ~6 MB |

本周 100 篇文档级别完全不用担心，但如果是平台级（百万文档），维度选择直接影响存储成本和检索延迟——这是你周六写 ADR 时可以考虑的点。

---

## 5. 三种相似度算法

### 5.1 余弦相似度（Cosine Similarity）← 本周主用

**公式**：cos(A, B) = (A · B) / (|A| × |B|)

**直觉**：只看两个向量的**方向**是否一致，不管长度。

- 值域：[-1, 1]，1 = 完全同方向，0 = 正交（无关），-1 = 完全反方向
- 实际 embedding 场景中值通常在 [0, 1] 之间（因为模型训练时向量都在正象限）

**类比**：两个人站在原点各指一个方向，余弦相似度量的是"两人手指的夹角有多小"，不管手臂伸多长。

**为什么最常用**：embedding 模型输出的向量长度（模）可能因文本长度不同而不同，余弦相似度自动忽略长度差异，只比语义方向。

### 5.2 点积（Dot Product）

**公式**：A · B = Σ(aᵢ × bᵢ)

**直觉**：方向 + 长度都考虑。

- 值域：(-∞, +∞)
- 如果向量已经归一化（模 = 1），点积 = 余弦相似度（因为分母为 1）

**什么时候用**：当你认为"向量长度也有意义"时（比如某些模型用长度表示确定性）。大部分 embedding 场景下，归一化后用点积和用余弦效果一样。

### 5.3 欧氏距离（Euclidean Distance / L2）

**公式**：d(A, B) = √(Σ(aᵢ - bᵢ)²)

**直觉**：两点之间的直线距离。

- 值域：[0, +∞)，0 = 完全重合，越大越不相似
- 注意：这是**距离**不是**相似度**（越小越相似，和余弦反过来）

**什么时候用**：向量数据库做 ANN（近似最近邻）检索时，底层常用 L2 距离，因为计算快。但如果向量已归一化，L2 距离排序和余弦相似度排序结果完全一致。

### 对比表

| 算法 | 衡量什么 | 值域 | 越大越相似？ | 是否受向量长度影响 |
|------|----------|------|:---:|:---:|
| 余弦 | 方向夹角 | [-1, 1] | 是 | 否（自动归一化） |
| 点积 | 方向 + 长度 | (-∞, +∞) | 是 | 是 |
| 欧氏 | 空间直线距离 | [0, +∞) | **否（越小越相似）** | 是 |

**本周结论**：用余弦。原因：embedding 模型输出向量长度不一定归一化，余弦自动忽略长度差异，最稳定。`embed_demo.py` 里的 `cosine()` 函数就是这个。

---

## 6. 和你已有经验的对照（帮助理解）

| Embedding 概念 | 你熟悉的类比 |
|---|---|
| 文本 → 向量 | 类似给每条记录算一个 fingerprint/hash，但这个 hash 是"语义保序"的 |
| 向量检索（ANN） | 类似数据库的空间索引（R-tree），只不过维度从 2D/3D 变成 1024D |
| 余弦相似度 | 类似推荐系统里的协同过滤打分 |
| Embedding 模型 | 类似一个"特征提取服务"——输入原始数据，输出特征向量 |
| 向量库（Chroma） | 类似 Elasticsearch，但索引结构从倒排索引变成 HNSW/IVF |

---

## 7. 读完自检（合上文件，心里回答）

1. embedding 和 token 的关系一句话说清？
2. 1024 维是什么意思？谁决定的？
3. 余弦和点积的核心区别？什么时候结果一样？
4. 为什么"狗"和"宠物"的向量比"狗"和"汽车"近？（训练机制一句话）

能答上来就去写 notes.md，答不上来翻回对应章节再看一遍。
